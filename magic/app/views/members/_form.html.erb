<%= form_for(@member) do |f| %>
  <% if @member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@member.errors.count, "error") %> prohibited this member from being saved:</h2>

      <ul>
      <% @member.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tournament %><br />
    <%= f.collection_select :tournament_id, Tournament.all, :id, :nombre, :prompt => true %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.collection_select :user_id, User.all, :id, :nombre, :prompt => true %>
  </div>
  <div class="field">
    <%= f.label :ganador %><br />
    <%= f.check_box :ganador %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
